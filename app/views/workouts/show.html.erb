<div class="col-5 mx-auto">
  <div class="workout_header mt-3">
    <h1>記録詳細</h1>
  </div><!-- /.workout_header -->

  <h2 class="mt-3"><%= @exercise.name %></h2>

  <nav class="navbar navbar-expand-sm my-3">
    <ul class="navbar-nav">
      <li class="nav-item"><%= link_to '一覧', workouts_path(exercise_id: @exercise.id), class: 'nav-link btn btn-default' %></li>
      <li class="nav-item"><%= link_to 'メニュー', menus_path, class: 'nav-link btn btn-default' %></li>
    </ul>
  </nav>

  <div class="exercise_log_content">
    <div class="form-group">
      <p>トレーニング日</p>
      <%= @workout.created_at.strftime("%Y-%m-%d %H:%M") %>
    </div>

    <table class="table table-hover">
      <thead>
        <tr>
          <th>set</th>
          <th>weight</th>
          <th>rep</th>
        </tr>
      </thead>

      <tbody>
        <% @exercise_logs.each do |exercise_log|%>
          <tr>
            <td><%= exercise_log.set %></td>
            <td><%= exercise_log.weight %></td>
            <td><%= exercise_log.rep %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="form-group">
      <p>コンディション</p>
      <p><%= @workout.condition_i18n %></p>
    </div><!-- /.form-group -->

    <div class="form-group">
      <p>メモ</p>
      <p><%= @workout.memo %></p>
    </div><!-- /.form-group -->
  </div>
</div>
